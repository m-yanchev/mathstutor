service: graphql-api
package:
  patterns:
    - '!**'
    - '!**.test.*'
  individually: true
plugins:
  - serverless-plugin-typescript
provider:
  name: aws
  region: eu-central-1
  runtime: nodejs14.x
  timeout: 15
  lambdaHashingVersion: 20201221
  layers:
    - arn:aws:lambda:eu-central-1:477719599351:layer:libs:46
    - arn:aws:lambda:eu-central-1:477719599351:layer:nodeModules:42
functions:
  gateway:
    handler: handlers/gateway/index.handler
    package:
      patterns:
        - handlers/gateway/*
    events:
      - http:
          path: /gateway
          method: post
          cors:
            origin: https://localhost:9000
            headers:
              - Content-Type
              - X-Amz-Date
              - Authorization
              - X-Api-Key
              - X-Amz-Security-Token
              - X-Amz-User-Agent
            allowCredentials: true
  profile:
    handler: handlers/profile/index.handler
    package:
      patterns:
        - handlers/profile/*
    events:
      - http:
          path: /profile
          method: post
          cors: true
  appointment:
    handler: handlers/appointment/index.handler
    package:
      patterns:
        - handlers/appointment/*
    events:
      - http:
          path: /appointment
          method: post
          cors: true
  course:
    handler: handlers/course/index.handler
    package:
      patterns:
        - handlers/course/*
    events:
      - http:
          path: /course
          method: post
          cors: true
  lesson:
    handler: handlers/lesson/index.handler
    package:
      patterns:
        - handlers/lesson/*
    events:
      - http:
          path: /lesson
          method: post
          cors: true
  test:
    handler: handlers/test/index.handler
    package:
      patterns:
        - handlers/test/*
    events:
      - http:
          path: /test
          method: post
          cors: true
  testResult:
    handler: handlers/testResult/index.handler
    package:
      patterns:
        - handlers/testResult/*
    events:
      - http:
          path: /test-result
          method: post
          cors: true